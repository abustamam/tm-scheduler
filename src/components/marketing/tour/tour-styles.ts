// Class strings shared by more than one `/tour` surface (#867), so the two
// copies of each cannot drift apart.

/** The "Beta" pill: on the assistant demo and on its scene's headline. */
export const BETA_PILL_CLASS =
	"rounded-full bg-warning-strong px-2.5 py-0.5 font-extrabold font-sans text-[11px] text-on-accent-fill uppercase tracking-[0.06em]";

/** A text-styled button: the demos' "Reset" and "Vote again". */
export const TEXT_BUTTON_CLASS =
	"cursor-pointer font-semibold text-[var(--lagoon-deep)] underline-offset-2 hover:underline";
